# Components

Централизованная библиотека компонентов проекта Albero.

## Структура

### 📦 UI Components (Design System)
Переиспользуемые компоненты дизайн-системы в `@/ui/`:
- **Button** - кнопки с вариантами и размерами
- **Input** - поля ввода с подсказками
- **Card** - карточки для контента
- **Avatar** - аватары пользователей
- **Select** - выпадающие списки с поиском
- **GenderSelect** - селектор пола
- **Modal** - модальные окна
- **Loader** - индикаторы загрузки

**Примечание:** UI компоненты вынесены на уровень `src/ui/` для лучшей организации.

### 🏗️ Layout Components
Компоненты структуры приложения:
- **Header** - шапка сайта с навигацией
- **SplashScreen** - заставка при входе

### 👤 Person Components
Компоненты для работы с персонами:
- **PersonProfile** - профиль персоны
- **PersonForm** - форма создания/редактирования
- **PersonalInfo** - личная информация
- **PersonTimeline** - временная шкала событий
- **PersonInfoRow** - строка информации в таблице

### 👨‍👩‍👧‍👦 Relative Components
Компоненты для отображения родственников:
- **RelativeCard** - карточка родственника
- **RelativeInfo** - информация о родственниках
- **RelativesSection** - секция родственников (родители/дети/супруги)

### 🔍 Search Components
Компоненты поиска:
- **SearchBox** - форма поиска с расширенными фильтрами

### 🌳 Other Components
Другие специализированные компоненты:
- **FamilyTree** - визуализация генеалогического древа
- **BirthdayList** - список дней рождений
- **EditableField** - редактируемое поле
- **AddRelativeModal** - модальное окно добавления родственника
- **LanguageSwitcher** - переключатель языков

### 🔌 Providers
React Context провайдеры в `@/providers/`:
- **ReduxProvider** - Redux store provider
- **AuthProvider** - провайдер аутентификации

**Примечание:** Провайдеры вынесены на уровень `src/providers/` для лучшей организации.

## Использование

### Импорт из центрального файла:
```typescript
import { Button, Input, Card, Header, PersonProfile } from '@/components';
```

### Импорт UI компонентов:
```typescript
import { Button, Input, Card } from '@/ui';
```

### Прямой импорт (если нужен только один компонент):
```typescript
import Header from '@/components/Header/Header';
```

## Принципы

### DRY (Don't Repeat Yourself)
- ✅ Переиспользуйте существующие компоненты
- ✅ Проверяйте `@/ui/` перед созданием нового компонента
- ✅ Используйте дизайн-систему для единообразия

### SOLID
- ✅ Один компонент = одна ответственность
- ✅ Компоненты открыты для расширения через props
- ✅ Компоненты закрыты для модификации

### i18n
- ✅ Все строки через `useTranslations()` хук
- ✅ Никаких хардкод строк в JSX
- ❌ НЕ использовать эмоджи в компонентах

## Структура компонента

Каждый компонент должен иметь:
```
ComponentName/
├── ComponentName.tsx       # Основной файл компонента
├── ComponentName.module.scss  # Стили (SCSS модули)
└── README.md (опционально) # Документация компонента
```

### Пример компонента:
```typescript
'use client';

import { useTranslations } from '@/i18n/useTranslations';
import styles from './ComponentName.module.scss';

interface ComponentNameProps {
  // Props interface
}

export default function ComponentName({ ...props }: ComponentNameProps) {
  const { t } = useTranslations();
  
  return (
    <div className={styles.component}>
      {t('common.example')}
    </div>
  );
}
```

## Стилизация

### Используйте SCSS модули:
```scss
@use '@/styles/variables' as *;
@use '@/styles/mixins' as *;

.component {
  padding: $spacing-md;
  
  @include md {
    padding: $spacing-lg;
  }
}
```

### Централизованные переменные:
- Цвета: `$primary-color`, `$bg-color`, `$text-color`
- Отступы: `$spacing-xs`, `$spacing-sm`, `$spacing-md`, `$spacing-lg`
- Радиусы: `$radius-sm`, `$radius-md`, `$radius-lg`, `$radius-xl`
- Тени: `$shadow-sm`, `$shadow-md`, `$shadow-lg`, `$shadow-xl`

### Миксины:
- `@include flex-center` - центрирование по flex
- `@include flex-between` - space-between layout
- `@include container` - контейнер с max-width
- `@include sm`, `@include md`, `@include lg` - медиа-запросы

## Тестирование

Перед коммитом:
1. ✅ Проверьте линтеры: `npm run lint`
2. ✅ Проверьте типы: `npm run build`
3. ✅ Проверьте i18n: все строки переведены?
4. ✅ Проверьте переиспользование: нет дублирования?

## Документация

Для сложных компонентов создавайте README.md с:
- Описанием компонента
- Примерами использования
- Описанием props
- Примерами кода

См. `./ui/README.md` для примера.

