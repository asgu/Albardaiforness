# UI Components

Библиотека переиспользуемых UI компонентов для проекта Albero.

## Быстрый старт

```tsx
import { Button, Input, Card, Avatar, Select, Loader } from '@/components/ui';

function MyComponent() {
  const [loading, setLoading] = useState(false);

  if (loading) {
    return <Loader text="Caricamento..." />;
  }

  return (
    <Card>
      <Avatar gender="male" size="lg" />
      <Input label="Name" placeholder="Enter name" />
      <Select
        label="Country"
        options={[
          { value: 'it', label: 'Italy', icon: '🇮🇹' },
          { value: 'ru', label: 'Russia', icon: '🇷🇺' },
        ]}
      />
      <Button variant="primary">Submit</Button>
    </Card>
  );
}
```

## Доступные компоненты

- **Button** - Кнопки с различными вариантами и состояниями
- **Input** - Поля ввода с лейблами, ошибками и иконками
- **Card** - Контейнеры для группировки контента
- **Avatar** - Аватары с поддержкой изображений и плейсхолдеров
- **Select** - Кастомизируемые выпадающие списки с поиском
- **Loader** - Анимированный индикатор загрузки

## Документация

Полная документация доступна в [DESIGN_SYSTEM.md](../../../DESIGN_SYSTEM.md)

## Принципы

1. **Простота** - Компоненты легко использовать
2. **Гибкость** - Поддержка кастомизации через props
3. **Доступность** - Следование a11y стандартам
4. **Производительность** - Оптимизация и React.memo где нужно
5. **Типизация** - Полная поддержка TypeScript

## Стилизация

Все компоненты используют SCSS модули и переменные из `@/styles/variables`.

Для кастомизации используйте:
- CSS классы через `className` prop
- SCSS переменные в `src/styles/_variables.scss`
- SCSS миксины в `src/styles/_mixins.scss`

